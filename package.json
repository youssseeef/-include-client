{
  "name": "car-project-client",
  "version": "1.0.0",
  "description": "This will fetch the data from the serial connection, transfer data from/to server and display a map",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "youssseeef",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "request": "^2.83.0",
    "serialport": "^6.0.4"
  }
}
